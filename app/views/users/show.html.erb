<div class="jumbotron text-center">

</div>

<div class="container">

	<h1><%= link_to "#{current_user.name}", user_path(@user) %></h1>

	<!-- image_tag @user.image.url(:thumb) if @user.image? -->

	<h3><%= avatar_for(@user) %><%= @user.status %></h3>

	<%= link_to 'Edit', edit_user_path(@user) %>


</div>